// Scenario: 03 - Navigate to Kyani Shop from Manage Tickets page

module.exports = function () {

    this.Then(/^he sees one ticket card$/, function (callback) {
        callback(null, 'pending');
    });
    this.Then(/^he sees the "([^"]*)" label$/, function (arg1, callback) {
        callback(null, 'pending');
    });
    this.Then(/^he sees the ticket number$/, function (callback) {
        callback(null, 'pending');
    });
    this.Then(/^he sees the register icon$/, function (callback) {
        callback(null, 'pending');
    });
    this.Then(/^he sees the "([^"]*)" option$/, function (arg1, callback) {
        callback(null, 'pending');
    });

};
